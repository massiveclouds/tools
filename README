bootstrap-armada:
	configure a node to join armada's configuration management services

bootstrap-sync:
	configure a node to replicate data to cloud block storage

clients:
	client specific resources

